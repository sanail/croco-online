# 🐊 Игра "Крокодил" (Crocodile Game)

Онлайн игра "Крокодил", где игроки показывают слова жестами, а другие пытаются их угадать.

## 🚀 Технологический стек

- **Backend**: Java 21, Spring Boot 3.2.0
- **Frontend**: Thymeleaf, HTML/CSS/JavaScript
- **База данных**: PostgreSQL 15
- **Миграции**: Liquibase
- **Сборка**: Maven
- **Контейнеризация**: Docker & Docker Compose

## 📋 Требования

- Java 21 или выше
- Maven 3.8+
- PostgreSQL 15 (или Docker)
- Docker & Docker Compose (опционально)

## 🎮 Основной функционал

- ✅ Создание игровых комнат с уникальной ссылкой
- ✅ Выбор темы для генерации слов
- ✅ Система очков и подсчёт результатов
- ✅ Автоматическая смена ведущего при угадывании
- ✅ Поддержка нескольких режимов (оффлайн/онлайн)
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Real-time обновления через polling
- ✅ Проверка угадывания с учётом опечаток (Levenshtein distance)

## 🎯 Темы слов

- Животные
- Профессии
- Предметы быта
- Фильмы и сериалы
- Еда и напитки
- Спорт
- Города и страны

## 📦 Установка и запуск

### Вариант 1: Запуск через Docker Compose (рекомендуется)

```bash
# Клонировать репозиторий
git clone <repository-url>
cd croco-online

# Запустить приложение и базу данных
docker-compose up --build

# Приложение будет доступно по адресу: http://localhost:8080
```

### Вариант 2: Локальный запуск

#### 1. Настроить PostgreSQL

```bash
# Создать базу данных
createdb crocodile_db

# Или через psql
psql -U postgres
CREATE DATABASE crocodile_db;
CREATE USER crocodile_user WITH PASSWORD 'crocodile_pass';
GRANT ALL PRIVILEGES ON DATABASE crocodile_db TO crocodile_user;
```

#### 2. Настроить переменные окружения (опционально)

```bash
export DB_HOST=localhost
export DB_PORT=5432
export DB_NAME=crocodile_db
export DB_USERNAME=crocodile_user
export DB_PASSWORD=crocodile_pass
export SERVER_PORT=8080
```

#### 3. Запустить приложение

```bash
# Собрать проект
mvn clean package

# Запустить
java -jar target/crocodile-game-1.0.0.jar

# Или через Maven
mvn spring-boot:run
```

### Вариант 3: Только PostgreSQL в Docker

```bash
# Запустить только базу данных
docker-compose up postgres

# В другом терминале запустить приложение
mvn spring-boot:run
```

## 🧪 Тестирование

```bash
# Запустить все тесты
mvn test

# Запустить конкретный тест
mvn test -Dtest=GameServiceTest
```

## 📁 Структура проекта

```
croco-online/
├── src/
│   ├── main/
│   │   ├── java/com/crocodile/
│   │   │   ├── controller/          # REST и View контроллеры
│   │   │   ├── service/              # Бизнес-логика
│   │   │   │   └── wordprovider/     # Strategy Pattern для генерации слов
│   │   │   ├── repository/           # JPA репозитории
│   │   │   ├── model/                # JPA entities
│   │   │   ├── dto/                  # Data Transfer Objects
│   │   │   ├── exception/            # Обработка исключений
│   │   │   ├── util/                 # Утилиты
│   │   │   └── CrocodileGameApplication.java
│   │   └── resources/
│   │       ├── db/changelog/         # Liquibase миграции
│   │       ├── static/               # CSS, JS
│   │       ├── templates/            # Thymeleaf шаблоны
│   │       └── application.yml       # Конфигурация
│   └── test/                         # Тесты
├── docker-compose.yml
├── Dockerfile
├── pom.xml
└── README.md
```

## 🎲 Как играть

1. **Создайте комнату**: Перейдите на главную страницу, выберите тему и создайте комнату
2. **Пригласите друзей**: Поделитесь ссылкой на комнату с друзьями
3. **Присоединитесь**: Введите своё имя для входа в игру
4. **Ведущий**: Первый зашедший становится ведущим
5. **Генерация слова**: Ведущий нажимает кнопку для получения нового слова
6. **Показ слова**: Ведущий показывает слово жестами (оффлайн) или через видео (онлайн)
7. **Угадывание**: Другие игроки вводят свои варианты ответа
8. **Смена ведущего**: Угадавший игрок становится новым ведущим и получает 10 очков!

## 🔧 Конфигурация

Основные настройки в `application.yml`:

```yaml
game:
  room:
    code-length: 6                    # Длина кода комнаты
    inactive-timeout-minutes: 60      # Время до автоматической очистки неактивных комнат
  session:
    cookie-name: CROCODILE_SESSION
    cookie-max-age: 86400             # 24 часа
  score:
    points-per-win: 10                # Очков за угадывание
```

## 🔌 API Endpoints

### Room Management

- `POST /api/rooms` - Создать новую комнату
- `GET /api/rooms/{code}/state` - Получить состояние комнаты
- `GET /api/rooms/themes` - Получить список доступных тем

### Game Actions

- `POST /api/rooms/{code}/join` - Присоединиться к комнате
- `POST /api/rooms/{code}/guess` - Отправить вариант ответа
- `POST /api/rooms/{code}/assign-winner` - Назначить победителя (только ведущий)
- `POST /api/rooms/{code}/new-word` - Сгенерировать новое слово (только ведущий)
- `POST /api/rooms/{code}/leave` - Покинуть комнату

### Views

- `GET /` - Главная страница
- `GET /room/{code}` - Страница игровой комнаты

## 🔮 Будущие улучшения

- [ ] Интеграция с YandexGPT для генерации слов
- [ ] Интеграция с LM Studio для локальной генерации слов
- [ ] WebSocket для real-time коммуникации (вместо polling)
- [ ] Таймер на раунд
- [ ] История игр с детальной статистикой
- [ ] Возможность создавать свои наборы слов
- [ ] Встроенная видеосвязь
- [ ] Режим рисования (как в Pictionary)
- [ ] Мультиязычная поддержка

## 📝 Лицензия

MIT License

## 👥 Автор

Разработано для онлайн игры в "Крокодил" с друзьями и семьёй.

## 🐛 Сообщить о проблеме

Если вы нашли баг или у вас есть предложение по улучшению, пожалуйста, создайте issue в репозитории.

